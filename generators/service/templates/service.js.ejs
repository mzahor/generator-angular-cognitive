function <%= servicePascalName %>ServiceRegistrator() {
    return <%= servicePascalName %>Service;

    function <%= servicePascalName %>Service() {
        'ngInject';

        var service = {
            dummyMethod
        };

        function dummyMethod() {
            return 'dummy';
        }

        return service;
    }
}

export default {
    name: '<%= servicePascalName %>Service',
    type: 'service',
    fn: <%= servicePascalName %>ServiceRegistrator,
}
