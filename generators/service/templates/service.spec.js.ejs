/* global module */

'use strict';

describe('Unit: <%= servicePascalName %>Service', function() {

    let service;

    beforeEach(function() {
        angular.mock.module('app.<%= moduleName %>');

        angular.mock.inject((_<%= servicePascalName %>Service_) => {
            service = _<%= servicePascalName %>Service_;
        });
    });

    describe('dummyMethod', function() {
        it('should return dummy string', function() {
            let result = service.dummyMethod();
            expect(result).toBe('dummy');
        });
    });

});
